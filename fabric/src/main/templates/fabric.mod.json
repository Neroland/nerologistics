{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "NeroLogistics - multiloader build (Fabric).",
  "authors": ["${mod_authors}"],
  "license": "${mod_license}",
  "accessWidener": "nerologistics.accesswidener",
  "environment": "*",
  "entrypoints": {
    "main": ["za.co.neroland.nerologistics.fabric.NeroLogisticsFabric"],
    "client": ["za.co.neroland.nerologistics.fabric.NeroLogisticsFabricClient"]
  },
  "mixins": [
    "nerologistics-common.mixins.json",
    "nerologistics.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "minecraft": ">=${minecraft_version}",
    "java": ">=21",
    "fabric-api": "*"
  }
}
